server:
  port: 7002
spring:
  application:
    name: microservice-consumer-feign-Hystrix

eureka:
  client:
    service-url:
      defaultZone: http://peer1:8001/eureka/

feign:
  hystrix:
    enabled: true
